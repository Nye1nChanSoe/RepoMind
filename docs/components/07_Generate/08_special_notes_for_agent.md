# Generate: Special Notes For Agent

- keep this layer at "proposed change" scope, not "apply changes" scope
- preserve the JSON-only response contract
- if diff behavior changes, update both model expectations and local fallback behavior
- model-routing changes here must stay aligned with `MODEL_GENERATE`
