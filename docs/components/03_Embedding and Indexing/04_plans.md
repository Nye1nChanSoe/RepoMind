# Embedding and Indexing: Plans

## Current Status

- implemented with local embeddings and persistent Chroma collections

## Planned Work

- add caching for repeated repository runs
- reduce unnecessary re-indexing when repository content has not changed
- add dedicated tests with lightweight embedding and Chroma fakes
- compare multiple local embedding models against the evaluation set
