# Embedding and Indexing: Special Notes For Agent

- keep embedding local unless the product explicitly changes that rule
- treat stored metadata keys as a shared contract with retrieval
- avoid introducing retrieval formatting concerns into this layer
- if the embedding model changes, update docs and dependency expectations together
