# Iteration 06: Next Steps

## Likely Follow-Up Work

1. Compare multiple embedding models on your evaluation set.
2. Add optional hosted embedding providers behind config if local quality remains limiting.
3. Surface the active embedding model in the UI for easier benchmarking.
4. Add incremental re-indexing keyed by repository content and embedding model.

## Exit Criteria For Iteration 07

- embedding model choice becomes part of the evaluation workflow
- retrieval quality can be tuned by config instead of implementation edits
